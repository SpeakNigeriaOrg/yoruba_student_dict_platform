# Generated candidate videos awaiting review - regenerated on demand, never
# a second copy of anything the database already has once accepted.
candidates/

# setuptools editable-install build artifact, regenerated by `uv sync`.
*.egg-info/
