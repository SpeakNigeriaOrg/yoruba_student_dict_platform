# Node
node_modules/
dist/
build/
.vite/
*.log
npm-debug.log*
*.tsbuildinfo

# api/'s self-contained vendored copy of shared's built output (see
# api/package.json's file:./vendor/shared dependency) - regenerated by
# build:api/test:api, not committed
api/vendor/

# Env / secrets
.env
.env.*
!.env.example
local.settings.json
*.local.settings.json

# Python (vad-service)
__pycache__/
*.pyc
.venv/
venv/

# Azure SWA CLI
.swa/

# Generated Wiktionary drafts (scripts/exportWiktionaryDrafts.mjs) - regenerated
# from the database on demand, and a draft committed here would be a second,
# staler copy of what the database already says
out/

# OS
.DS_Store
