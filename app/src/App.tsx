// Placeholder shell - the real screens (Definitions/Spelling & Tone/Etymology
// review, assignment view, contribution queue, audio recorder) aren't built
// yet. This exists so the Vite/React/TS toolchain has something real to
// build and serve while those are implemented incrementally. See
// src/audio/segmentSyllables.ts for the one piece of real, tested logic
// that exists so far.

export default function App() {
  return (
    <main>
      <h1>Yoruba Student Dictionary - Curation Platform</h1>
      <p>Under construction.</p>
    </main>
  );
}
